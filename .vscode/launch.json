{
    "version": "0.2.0",
    "configurations": [
        
        {
            "name": "FastAPI 运行",
            "type": "debugpy",
            "request": "launch",
            "module": "uvicorn",
            "cwd": "${workspaceFolder}/backend",
            "args": [
                "main:app",
                "--reload",
                "--host", "localhost",
                "--port", "8000"
            ],
            "console": "integratedTerminal"
        }
    ]
}